<!DOCTYPE html>
<html lang="es">
  <!-- 
    La etiqueta "head" contiene información sobre el documento, como el título, 
    el conjunto de caracteres y los enlaces a hojas de estilo.
  -->
  <head>
    <!-- 
      La etiqueta "meta" proporciona información sobre el documento, 
      como el conjunto de caracteres y la codificación.
    -->
    <meta charset="UTF-8">
    <!-- 
      La etiqueta "title" especifica el título del documento, 
      que se muestra en la barra de título del navegador.
    -->
    <title>Notas</title>
    <!-- 
      La etiqueta "link" enlaza una hoja de estilo externa al documento.
      En este caso, enlazamos la hoja de estilo "styles.css".
    -->
    <link rel="stylesheet" href="styles.css">
  </head>
  <!-- 
    La etiqueta "body" contiene el contenido del documento.
  -->
  <body>
    <!-- 
      La etiqueta "h1" indica un título de nivel 1.
    -->
    <h1>Notas</h1>
    <!-- 
      La etiqueta "form" define un formulario.
      En este caso, creamos un formulario con un campo de texto y un botón.
    -->
    <form id="note-form">
      <!-- 
        La etiqueta "label" proporciona una etiqueta para un campo de formulario.
      -->
      <label for="note-text">Nota:</label>
      <!-- 
        La etiqueta "textarea" define un campo de texto multilínea.
      -->
      <textarea id="note-text" name="note-text"></textarea>
      <!-- 
        La etiqueta "button" define un botón.
      -->
      <button type="submit">Crear nota</button>
    </form>
    <!-- 
      La etiqueta "ul" define una lista no ordenada.
      En este caso, creamos una lista para mostrar las notas.
    -->
    <ul id="note-list">
      <!-- 
        La lista de notas se mostrará aquí.
      -->
    </ul>
    <!-- 
      La etiqueta "script" enlaza un archivo de script externo al documento.
      En este caso, enlazamos el archivo de script "script.js".
    -->
    <script src="script.js"></script>
  </body>
</html>